[build-system]
requires = ["setuptools", "wheel"]

[project]
name = "fair-ai-demo"
version = "1.0.0"
dependencies = [
    "streamlit>=1.28.0",
    "pandas>=2.0.0",
    "numpy>=1.21.0",
    "scikit-learn>=1.0.0",
    "joblib>=1.2.0",
]
